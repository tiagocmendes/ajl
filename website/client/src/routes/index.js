const baseUrl = 'http://localhost:8080';
export const routes = {
    login: `${baseUrl}/login`,
    games: `${baseUrl}/games`,
    knockouts: `${baseUrl}/knockouts`,
    groups: `${baseUrl}/groups`,
}